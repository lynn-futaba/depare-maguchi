[Start: Gate List Page]
  |
  |-- API: GET /api/gates  --> Fetch gates, lines, stock info
  |
  v
[Display Gates Grid]
  |
  | User taps a gate
  |-- API: GET /api/gates/{gateId}/parts  --> Fetch parts info at selected gate
  |
  v
[Gate Detail Page/Modal]
  |-------------------------------------------------|
  | Display parts list with quantities and controls |
  | "+" / "−" buttons per part                        |
  |-------------------------------------------------|
  | For each quantity change:                         |
  |-- API: POST /api/gates/{gateId}/parts/{partId}  |
  |     with {quantityChange: +1 or -1}              |
  |-------------------------------------------------|
  | Button: "Takeout Complete"                        |
  |-- API: POST /api/gates/{gateId}/takeoutComplete |
  |-------------------------------------------------|
  | Button: "Work Complete"                           |
  |-- API: POST /api/gates/{gateId}/workComplete    |
  |-------------------------------------------------|
  |
  | On success: return to Gate List Page or next gate
  v
[Kanban Operations Page (tab/section)]
  |
  |-- API: GET /api/kanban/requests  --> Get Kanban removal/insertion requests
  |
  | Operator confirms Kanban removal/insertion:
  |-- API: POST /api/kanban/{requestId}/complete
  |
  v
[End / Loop back to main page]